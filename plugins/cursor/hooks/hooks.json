{
  "version": 1,
  "hooks": {
    "preToolUse": [
      {
        "command": "/bin/sh \"${CURSOR_PLUGIN_ROOT}/runtime/hook.sh\" cursor || printf '%s\\n' '{\"permission\":\"allow\"}'",
        "matcher": "Read|Shell",
        "timeout": 20,
        "failClosed": false
      }
    ]
  }
}
